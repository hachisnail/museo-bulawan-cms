import React from 'react';

export default function ArticlesCMS() {
    // The Payload CMS iframe is now rendered persistently in MainLayout.jsx
    // This prevents slow load times when switching tabs.
    return null;
}
